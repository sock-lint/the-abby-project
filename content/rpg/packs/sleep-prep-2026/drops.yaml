# Sleep Prep 2026 — drops
# Only the consumable food drops from activity. The frame + title are
# earned via their quests (see quests.yaml reward_items + badges.yaml).
rules:
  - triggers: [habit_log]
    item_slugs: [food-chamomile-tea]
    weight: 3
    min_level: 0
