# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/

# Django
db.sqlite3
db.sqlite3-journal
# Catch ad-hoc dev sqlite DBs created via DATABASE_URL=sqlite:///some.db
# (e.g. dev_local.db, tmp_test.db, tmp_migration_check.db) so they don't
# get committed by accident — same shape as the entries above.
*.db
*.db-journal
media/
staticfiles/
*.log

# Environment
.env

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node (for frontend)
node_modules/
frontend/dist/

# Docker
docker-compose.override.yml

# Worktrees
.worktrees/
/.playwright-mcp

# Licensed RPG sprite sheets — not redistributable. The sliced derivatives
# under frontend/src/assets/rpg-sprites/ ARE committed; the raw packs stay
# local. See content/rpg/README.md for where to download them.
reward-icons/
/.claude/worktrees
